Drop demo screenshots here with these filenames (referenced from the root `README.md`):

- `setup.png` — the Setup screen (job title + resume upload)
- `session.png` — a live interview session (avatar + candidate video + transcript)
- `summary.png` — the Summary screen (stats, self-rated readiness, transcript)

Any image format works as long as the filename (and extension) matches what's linked
in the root README — update both together if you rename a file.
